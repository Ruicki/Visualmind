[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install --production=false"]

[phases.build]
cmds = ["node scripts/init_prod_db.js || echo 'DB init skipped'"]

[start]
cmd = "node src/server.js"
