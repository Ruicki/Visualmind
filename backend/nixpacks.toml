[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm ci"]

# La BD se inicializa al arrancar el servidor (src/config/initDb.js),
# porque durante el build la red privada de la BD no está disponible.

[start]
cmd = "node src/server.js"
