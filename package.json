{
  "name": "visualmind-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Visualmind Monorepo for Vercel and Railway",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd frontend && npm install && npm run build"
  }
}
